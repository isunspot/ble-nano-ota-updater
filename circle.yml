machine:
    node:
        version: 4.2.4

dependencies:
    cache_directories:
        - app/bower_components
        - node_modules
        - plugins
    override:
        - npm prune
        - npm install

        - npm install -g bower
        - bower prune
        - bower install

        - gulp --cordova "prepare"

test:
    override:
        - gulp eslint
